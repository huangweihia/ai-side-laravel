# 🚨 快速修复指南

## 当前问题
头像上传功能报错

## ✅ 已确认正常的配置

1. **路由已定义** ✅
   ```php
   Route::post('profile/upload-avatar', [HomeController::class, 'uploadAvatar']);
   ```

2. **上传目录已创建** ✅
   ```
   public/avatars/ (权限 777)
   ```

3. **CSRF Token 已添加** ✅
   ```html
   <meta name="csrf-token" content="{{ csrf_token() }}">
   ```

4. **控制器方法存在** ✅
   ```php
   public function uploadAvatar(Request $request) { ... }
   ```

## 🔧 立即执行

```powershell
cd D:\lewan\openclaw-data\workspace\ai-side-laravel

# 1. 强制清理所有缓存
docker compose exec php php artisan cache:clear
docker compose exec php php artisan config:clear
docker compose exec php php artisan view:clear

# 2. 确认目录权限
docker compose exec php chmod -R 777 public/avatars
docker compose exec php chmod -R 777 storage/

# 3. 重启容器（可选）
docker compose restart
```

## 🧪 测试步骤

1. **清除浏览器缓存**
   - Ctrl+Shift+Delete 清除缓存
   - 或使用无痕模式

2. **访问个人中心**
   - `http://localhost:8081/dashboard`

3. **上传头像**
   - 点击头像右下角的 📷 图标
   - 选择小图片测试（< 500KB）

## 📋 如果还报错

请提供以下信息：

1. **错误截图** - 页面显示什么错误？
2. **浏览器控制台** - F12 → Console 标签的错误
3. **Network 请求** - F12 → Network 标签，上传请求的响应

## 🔍 常见错误代码

- **419** - CSRF Token 问题 → 刷新页面
- **500** - 服务器错误 → 查看日志
- **404** - 路由不存在 → 清理缓存
- **413** - 文件太大 → 使用小图片

---

**更新时间：** 2026-03-26 13:17
